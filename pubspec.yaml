name: flutter_application_1
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # 'assets:' SUDAH DIHAPUS DARI SINI

  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
    
  flutter_lints: ^3.0.0

# Bagian ini adalah bagian yang benar
flutter:
  uses-material-design: true

  # ============================================
  # SAYA PINDAHKAN KE SINI
  # Ini adalah lokasi yang benar
  assets:
    - assets/images/
  # ============================================

  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf